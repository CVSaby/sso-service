version: '3'

tasks:
  migrate:
    cmds:
      - go run ./cmd/migrator --migrations-path=./migrations --config=./config/local.yaml

  docker-build:
    cmds:
      - docker build -t ssoservice .

  docker-run:
    cmds:
      - docker run -d --name sso-service -p 4000:4000 ssoservice
